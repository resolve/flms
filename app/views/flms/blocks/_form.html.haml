= form_for [@page, @block], html: {class: 'form-horizontal'} do |f|
  %fieldset
    %legend= caption

  - if @block.errors.any?
    #error_explanation
      %h2= "#{pluralize(@block.errors.count, "error")} prohibited this block from being saved:"
      %ul
        - @block.errors.full_messages.each do |msg|
          %li= msg

  .control-group
    = f.label :name, class: 'control-label'
    .controls
      = f.text_field :name, autofocus: true
      %span.help-inline The internal name of this block.

  .control-group
    .controls
      .actions
        = f.submit class: 'btn btn-primary'
        &nbsp; &nbsp; &nbsp;
        = link_to 'Back', pages_blocks_path(@page)


