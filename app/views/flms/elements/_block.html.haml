.block{ id: block.name }
  - block.layers.is_active.ordered_by_scroll_start.each do |layer|
    = render partial: layer.view_object.view_partial_name, locals: { layer: layer, scroll_offset: scroll_offset }
