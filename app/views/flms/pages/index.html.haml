%br
%h4 All pages

%table.table.table-striped.table-hover
  %thead
    %tr
      %th Title
      %th

  %tbody
    - @pages.each do |page|
      %tr
        %td= link_to page.title, edit_page_path(page)
        %td= link_to 'delete', page_path(page), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Page', new_page_path, class: 'btn btn-success'

